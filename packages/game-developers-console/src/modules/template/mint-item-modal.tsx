import { Modal } from 'components/modal'
import React from 'react'

interface IMintItemModal {
   templateId: number
}

export const MintItemModal: React.FC<IMintItemModal> = ({ templateId }) => {
   return (
      <Modal>

      </Modal>
   )
}
